name: Validate Threat Model

on:
  push:
    paths:
      - 'threat-models/*.json'

jobs:
  validate-threat-model:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Validate JSON format
        run: |
          jq . threat-models/model.json
